/**
 Write in this file strings like exports.mymodel = require('./mymodel'), where mymodel - files mymodel.js, located in this directory
 If you do this, you can use models = require('./models') and all functions will be reaches with model.models.myfunc()
 */
